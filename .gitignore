# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.vite/
*.tsbuildinfo

# Environment — never commit real secrets
.env
.env.*
!.env.example

# Test / coverage artifacts
coverage/
playwright-report/
test-results/
blob-report/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/

# Prisma generated client
apps/api/src/generated/

# Private notes — personal interview prep, never published
docs/private/

# TEMPORARY — remove this entry once the lockfile is regenerated off corporate VPN.
# The current lockfile resolves all 560 tarballs through an internal Microsoft
# registry proxy with SHA-1 integrity hashes. Committing it would break CI and
# leak internal hostnames. See docs/CHALLENGES.md.
pnpm-lock.yaml
